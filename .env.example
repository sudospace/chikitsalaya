PORT=8090
DATABASE_URL=postgres://chikitsalaya:chikitsalaya@localhost:5442/chikitsalaya?sslmode=disable
# Encrypts patient documents at rest under the "local" storage backend
# (the default). Generate your own: openssl rand -hex 32
CHIKITSALAYA_LOCAL_STORAGE_KEY=
